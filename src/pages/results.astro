---
/**
 * Results / Case Studies Page
 *
 * Client success stories and outcomes
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import { testimonials } from '../data/testimonials';

// TODO: Get real metrics from Keith
const metrics = [
  { value: '200+', label: 'Law Firms Served' },
  { value: '12+', label: 'Years Experience' },
  { value: '94%', label: 'Client Retention Rate' },
  { value: '10', label: 'Video Testimonials' },
];

// Organization Schema with testimonials
const resultsSchema = {
  '@context': 'https://schema.org',
  '@type': 'WebPage',
  name: 'Client Results and Case Studies',
  description: 'Real results from LEXGRO clients. See case studies, ROI metrics, and testimonials from personal injury, family law, and immigration firms.',
  url: 'https://lexgro.com/results/',
  mainEntity: {
    '@type': 'Organization',
    name: 'LEXGRO',
    url: 'https://lexgro.com',
    description: 'Legal marketing agency helping law firms grow with data-driven strategies.',
    review: testimonials.map((t) => ({
      '@type': 'Review',
      author: {
        '@type': 'Person',
        name: t.name,
        jobTitle: t.title,
        worksFor: {
          '@type': 'LegalService',
          name: t.firm,
          areaServed: t.practiceArea,
        },
      },
      ...(t.youtubeId && {
        video: {
          '@type': 'VideoObject',
          name: `${t.name} - LEXGRO Testimonial`,
          embedUrl: `https://www.youtube.com/embed/${t.youtubeId}`,
        },
      }),
    })),
  },
};
---

<BaseLayout
  title="Client Results and Case Studies"
  description="Real results from LEXGRO clients. See case studies, ROI metrics, and testimonials from personal injury, family law, and immigration firms."
  image="/og/results.png"
>
  <script type="application/ld+json" set:html={JSON.stringify(resultsSchema)} />

  <!-- Hero Section -->
  <section class="results-hero">
    <div class="container">
      <div class="hero-content">
        <span class="section-label">Results</span>
        <h1 class="hero-title">Real Results for Real Law Firms</h1>
        <p class="hero-subtitle">
          We measure success by one thing: your growth. Here's what we've helped law firms achieve.
        </p>
      </div>
    </div>
  </section>

  <!-- Metrics Section -->
  <section class="metrics-section">
    <div class="container">
      <div class="metrics-grid">
        {metrics.map((metric) => (
          <div class="metric-card">
            <span class="metric-value">{metric.value}</span>
            <span class="metric-label">{metric.label}</span>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Testimonials Section -->
  <section class="testimonials-section">
    <div class="container">
      <div class="section-header">
        <h2 class="section-title">What Our Clients Say</h2>
        <p class="section-subtitle">
          Hear directly from law firm owners who've worked with LEXGRO.
        </p>
      </div>

      <div class="testimonials-grid">
        {testimonials.map((testimonial) => (
          <div class="testimonial-card">
            <div class="testimonial-video">
              <div class="video-placeholder">
                <svg class="play-icon" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <circle cx="12" cy="12" r="11" stroke="currentColor" stroke-width="2"/>
                  <path d="M10 8l6 4-6 4V8z" fill="currentColor"/>
                </svg>
              </div>
            </div>
            <div class="testimonial-info">
              <h3 class="testimonial-name">{testimonial.name}</h3>
              <p class="testimonial-title">{testimonial.title} | {testimonial.practiceArea}</p>
              <p class="testimonial-firm">{testimonial.firm}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Approach Section -->
  <section class="approach-section">
    <div class="container">
      <div class="approach-content">
        <span class="section-label-dark">Our Approach</span>
        <h2 class="section-title-light">Why We Get Results</h2>
        <div class="approach-grid">
          <div class="approach-item">
            <h3>Deep Legal Industry Focus</h3>
            <p>We only work with law firms. Every strategy is built on what actually works in legal marketing, not generic advice.</p>
          </div>
          <div class="approach-item">
            <h3>Data-Driven Decisions</h3>
            <p>We track everything. Data backs every decision, and results hold every dollar accountable.</p>
          </div>
          <div class="approach-item">
            <h3>Long-Term Partnership</h3>
            <p>We're not here for quick wins. Our average client relationship spans years, not months.</p>
          </div>
          <div class="approach-item">
            <h3>Honest Communication</h3>
            <p>We tell you what's working, what isn't, and what to do about it. No sugarcoating, no vanity metrics.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="container">
      <div class="cta-content">
        <h2 class="cta-title">Ready to Write Your Success Story?</h2>
        <p class="cta-subtitle">
          Let's discuss how LEXGRO can help your firm achieve similar results.
        </p>
        <a href="/contact/" class="btn btn-primary btn-lg">
          Schedule a Free Consultation
        </a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Hero */
  .results-hero {
    padding: 5rem 0 3rem;
    background: linear-gradient(180deg, var(--color-bg-light) 0%, white 100%);
  }

  .hero-content {
    max-width: 650px;
  }

  .section-label {
    color: var(--color-primary);
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.75rem;
    display: block;
  }

  .section-label-dark {
    color: var(--color-mint);
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.75rem;
    display: block;
  }

  .hero-title {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1.25rem;
    color: var(--color-text-dark);
  }

  .hero-subtitle {
    font-size: 1.125rem;
    color: #4b5563;
    line-height: 1.6;
  }

  /* Metrics Section */
  .metrics-section {
    padding: 0 0 4rem;
    background: white;
  }

  .metrics-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1.5rem;
  }

  .metric-card {
    padding: 2rem;
    background: var(--color-bg-dark);
    border-radius: 16px;
    text-align: center;
  }

  .metric-value {
    display: block;
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--color-mint);
    margin-bottom: 0.5rem;
  }

  .metric-label {
    font-size: 0.9375rem;
    color: #b0b8b3;
  }

  /* Testimonials Section */
  .testimonials-section {
    padding: 5rem 0;
    background: white;
  }

  .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .section-title {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 700;
    color: var(--color-text-dark);
    margin-bottom: 0.75rem;
  }

  .section-title-light {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 700;
    color: white;
    margin-bottom: 2rem;
  }

  .section-subtitle {
    font-size: 1.0625rem;
    color: #4b5563;
  }

  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .testimonial-card {
    background: var(--color-bg-light);
    border-radius: 16px;
    overflow: hidden;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .testimonial-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.1);
  }

  .testimonial-video {
    position: relative;
    aspect-ratio: 16 / 9;
    background: linear-gradient(135deg, var(--color-bg-dark) 0%, #1a2e22 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
  }

  .video-placeholder {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .play-icon {
    width: 64px;
    height: 64px;
    color: white;
    opacity: 0.9;
    transition: opacity 0.2s ease, transform 0.2s ease;
  }

  .testimonial-card:hover .play-icon {
    opacity: 1;
    transform: scale(1.1);
  }

  .testimonial-info {
    padding: 1.5rem;
  }

  .testimonial-name {
    font-size: 1.125rem;
    font-weight: 700;
    color: var(--color-text-dark);
    margin-bottom: 0.25rem;
  }

  .testimonial-title {
    font-size: 0.875rem;
    color: var(--color-primary);
    font-weight: 500;
    margin-bottom: 0.25rem;
  }

  .testimonial-firm {
    font-size: 0.875rem;
    color: #4b5563;
  }

  /* Approach Section */
  .approach-section {
    padding: 5rem 0;
    background: var(--color-bg-dark);
  }

  .approach-content {
    max-width: 900px;
    margin: 0 auto;
    text-align: center;
  }

  .approach-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    text-align: left;
  }

  .approach-item h3 {
    font-size: 1.125rem;
    font-weight: 700;
    color: white;
    margin-bottom: 0.5rem;
  }

  .approach-item p {
    font-size: 0.9375rem;
    color: #b0b8b3;
    line-height: 1.6;
  }

  /* CTA */
  .cta-section {
    padding: 5rem 0;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-teal) 100%);
    text-align: center;
  }

  .cta-content {
    max-width: 500px;
    margin: 0 auto;
  }

  .cta-title {
    font-size: clamp(1.75rem, 4vw, 2.25rem);
    color: white;
    margin-bottom: 1rem;
  }

  .cta-subtitle {
    font-size: 1rem;
    color: #b0b8b3;
    margin-bottom: 2rem;
  }

  .cta-section .btn-primary {
    background: white;
    color: var(--color-primary);
  }
</style>
